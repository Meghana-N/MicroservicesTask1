---
server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/svnsairam/config-server-repo
          search-paths:
            - 'track*'
            - 'user*'